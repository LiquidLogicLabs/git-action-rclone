# Secrets for local act testing
# Copy this file to .act.secrets and fill in real values
# Do NOT commit .act.secrets with real tokens

# Required for workflows that use GITHUB_TOKEN
GITHUB_TOKEN=your-github-token-here

# Optional: for E2E tests against a real remote
# SFTP_PASSWORD=your-sftp-password
# AWS_ACCESS_KEY=your-aws-access-key
# AWS_SECRET_KEY=your-aws-secret-key
